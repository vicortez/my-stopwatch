# Client folder for my stopwatch app

This client was created using npm create vite@latest. It uses react, typescript, tailwind.

I opted for a flat folder structure, grouped by type of file, for simplicity.
